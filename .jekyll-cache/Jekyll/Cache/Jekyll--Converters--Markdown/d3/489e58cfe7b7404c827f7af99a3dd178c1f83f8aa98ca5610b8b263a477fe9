I"|<p>Files can be stored in a project’s <code class="language-plaintext highlighter-rouge">working directory</code> (which users see), the <code class="language-plaintext highlighter-rouge">staging area</code> (where the next commit is being built up) and the <code class="language-plaintext highlighter-rouge">local repository</code> (where commits are permanently recorded)</p>
:ET